# คู่มือการติดตั้งและเชื่อมต่อฐานข้อมูลออนไลน์ (Supabase & Google Sheets)
### ระบบยืม-คืนหนังสือ ห้องสมุดหมวดภาษาไทย โรงเรียนบรรหารแจ่มใสวิทยา ๓

ระบบนี้ได้รับการพัฒนาให้สามารถจัดเก็บข้อมูลออนไลน์แบบถาวรบน **Supabase (PostgreSQL)** และสามารถส่งประวัติการยืม-คืนไปยัง **Google Sheets** ของโรงเรียนได้โดยอัตโนมัติ

---

## ส่วนที่ 1: การเชื่อมต่อ Supabase PostgreSQL (ฐานข้อมูลหลัก - แนะนำ 100%)

Supabase เป็นฐานข้อมูล Cloud PostgreSQL ที่เปิดให้ใช้งาน **ฟรีตลอดชีพ (Free Tier)** ข้อมูลหนังสือและประวัติการยืม-คืนจะถูกจัดเก็บบนคลาวด์จริง ไม่หายเมื่อเปลี่ยนเครื่องหรือปิดเบราว์เซอร์

### ขั้นตอนการสร้างและเชื่อมต่อ:
1. **สมัครและสร้างโปรเจกต์:**
   - เข้าเว็บไซต์ [https://supabase.com](https://supabase.com) แล้วลงชื่อเข้าใช้งานด้วย GitHub หรือ Email
   - กดปุ่ม **New Project**
   - ตั้งชื่อโปรเจกต์ เช่น `bj3-library`
   - ตั้งรหัสผ่านฐานข้อมูล (Database Password)
   - เลือก Region: **Singapore (ap-southeast-1)** เพื่อความรวดเร็วในการเชื่อมต่อจากประเทศไทย
   - กด **Create new project** แล้วรอระบบจัดเตรียมฐานข้อมูลประมาณ 1-2 นาที

2. **สร้างตารางข้อมูล (Run SQL Script):**
   - ในแถบเมนูด้านซ้ายของ Supabase ให้คลิกที่ไอคอน **SQL Editor**
   - กดปุ่ม **New query**
   - เปิดไฟล์ `supabase_schema.sql` ในโปรเจกต์นี้ คัดลอกโค้ด SQL ทั้งหมดมาวางในช่อง
   - กดปุ่ม **RUN** (หรือกดปุ่ม `Ctrl + Enter`)
   - ระบบจะสร้างตาราง `books`, `transactions`, `wishlists`, `library_settings` และเพิ่มหนังสือตัวอย่างหมวดภาษาไทย 5 เล่มพร้อมใช้งานทันที

3. **นำ API Keys มาเชื่อมต่อกับระบบ:**
   - ใน Supabase ไปที่ **Project Settings** (ไอคอนฟันเฟืองด้านล่างซ้าย) -> **API**
   - คัดลอกค่า 2 อย่าง:
     1. **Project URL** (เช่น `https://abcdefghijkl.supabase.co`)
     2. **Project API keys (anon public)** (เช่น `eyJhbGciOiJIUzI1...`)
   - นำค่าทั้งสองมากรอกในระบบ ได้ 2 ช่องทาง:
     - **ช่องทางที่ 1 (สะดวกที่สุด):** เข้าเว็บระบบ -> ล็อกอินแอดมิน (`thaibj3` / `12123`) -> ไปที่แท็บ **"2. ฐานข้อมูลคลาวด์"** -> วาง URL และ Anon Key -> กด **"ทดสอบการเชื่อมต่อ"** -> กด **"บันทึกการตั้งค่าฐานข้อมูล"**
     - **ช่องทางที่ 2 (สำหรับ Deploy บน Vercel):** ไปที่ Vercel Dashboard -> โปรเจกต์ -> **Settings** -> **Environment Variables** แล้วเพิ่ม:
       - `SUPABASE_URL` = `<Project URL>`
       - `SUPABASE_ANON_KEY` = `<Anon Key>`
       - `NEXT_PUBLIC_SUPABASE_URL` = `<Project URL>`
       - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = `<Anon Key>`

---

## ส่วนที่ 2: การเชื่อมต่อ Google Sheets Webhook (ระบบเสริมสำหรับครู)

คุณครูสามารถเปิดดูและสั่งพิมพ์รายงานการยืม-คืนผ่าน Google Sheets ได้แบบ Real-time

### ขั้นตอนการติดตั้ง Google Apps Script:
1. เปิด [Google Drive](https://drive.google.com) แล้วสร้าง **Google Sheets** ใหม่ ตั้งชื่อว่า "ระบบยืมคืนห้องสมุดภาษาไทย บ.จ.๓"
2. ไปที่เมนูด้านบนเลือก **ส่วนขยาย (Extensions)** -> **Apps Script**
3. ลบโค้ดเริ่มต้นออกทั้งหมด แล้วเปิดไฟล์ `google_apps_script.js` ในโปรเจกต์นี้ คัดลอกโค้ดทั้งหมดมาวาง
4. กดปุ่ม **บันทึก (Save)**
5. กดปุ่มสีน้ำเงินด้านบนขวา **การทำให้ใช้งานได้ (Deploy)** -> **การทำให้ใช้งานได้ใหม่ (New deployment)**
6. คลิกรูปฟันเฟืองเลือกประเภท: **เว็บแอป (Web app)**
7. ตั้งค่าการเผยแพร่:
   - คำอธิบาย: `BJ3 Library Sync Webhook`
   - ดำเนินการในฐานะ (Execute as): **ฉัน (Me)**
   - ผู้ที่มีสิทธิ์เข้าถึง (Who has access): **ทุกคน (Anyone)** *(สำคัญมาก เพื่อให้ระบบส่งข้อมูลได้โดยไม่ต้องล็อกอินบัญชี Google ซ้ำ)*
8. กด **ทำให้ใช้งานได้ (Deploy)** และกดยืนยันสิทธิ์การเข้าถึง (Authorize access)
9. คัดลอก **URL เว็บแอป (Web app URL)** ที่ได้ (ขึ้นต้นด้วย `https://script.google.com/macros/s/.../exec`)
10. นำ URL มาวางในหน้าแอดมิน -> แท็บ **"2. ฐานข้อมูลคลาวด์"** -> ช่อง **Google Apps Script Web App URL** -> กดบันทึก

---

## ส่วนที่ 3: บัญชีผู้ดูแลระบบ (Admin Credentials)

- **ชื่อผู้ใช้งานเริ่มต้น:** `thaibj3` (หรือ `admin`)
- **รหัสผ่านเริ่มต้น:** `12123` (หรือ `1234`)
- สามารถเปลี่ยนชื่อผู้ใช้และรหัสผ่านได้ในหน้า แอดมิน -> แท็บ **"1. ตั้งค่าระบบ"**

---

## ส่วนที่ 4: การนำขึ้น Vercel (Deployment)

1. ทำการ Push โค้ดทั้งหมดขึ้น GitHub
2. เข้าสู่ [Vercel](https://vercel.com) แล้ว Import Repository
3. ใส่ Environment Variables ตามไฟล์ `.env.example` (SUPABASE_URL, SUPABASE_ANON_KEY)
4. กด **Deploy** ระบบจะพร้อมใช้งานออนไลน์ทันที 24 ชั่วโมง!
